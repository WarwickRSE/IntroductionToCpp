#include <iostream>

int main(){
	//You can use an if statement to make code happen on certain conditions
	//You have already seen this basic effect when using loops but here is how to do it manually
	
	int candidate=1;
	int test=4;

	if (test>candidate) std::cout << "Test is greater than candidate\n";
}
